name: e2e

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      # Playwright browsers (~1.5GB) are cached across runs; the key is tied to
      # package-lock.json, so any dependency bump that changes the Playwright
      # version busts the cache automatically.
      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      # OS packages (libevent, etc.) are NOT cached — a fresh runner never has
      # them — so they must be installed on every run. Only the ~1.5GB browser
      # download is skipped on a cache hit; browsers are restored by actions/cache
      # above and re-verified by `playwright install`.
      - name: Install Playwright OS dependencies
        run: npx playwright install-deps chromium webkit

      - name: Install Playwright browsers (cached)
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: npx playwright install chromium webkit

      # Builds the static bundle, then runs both headless projects in parallel
      # against `vite preview` (see playwright.config.js CI branch).
      - name: Run e2e suites
        run: npm run test:e2e:ci

      - name: Upload Playwright trace on failure
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 7
          if-no-files-found: ignore